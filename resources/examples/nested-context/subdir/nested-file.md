<!--
description: File in subdirectory without its own resource.json
whenToLoad: When you need to access nested content
importance: low
-->

# Nested File

This file is in a subdirectory that doesn't have its own resource.json.

It should still be accessible because the parent directory has a resource.json.
